# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'coffeescript', :input => 'src' , :output => 'app'
guard 'haml', :input => 'src/templates', :output => 'app/templates' do
  watch(/^.+\.html\.haml$/)
end
